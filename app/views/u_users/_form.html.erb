<%= form_with(model: u_user, local: true) do |form| %>
  <% if u_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(u_user.errors.count, "error") %> prohibited this u_user from being saved:</h2>

      <ul>
        <% u_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :full_name %>
    <%= form.text_field :full_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
